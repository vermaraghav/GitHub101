from tkinter import *
root = Tk()

label1 = Label(root, text="First Name", bg="Red", fg="grey")
label1.pack(fill=X)

label2 = Label(root, text="Last Name", bg="blue", fg="orange")
label2.pack(fill=Y, side="right")

root.mainloop()
